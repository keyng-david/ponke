{
  "name": "websocket-server",
  "version": "1.0.0",
  "main": "webSocketServer.ts",
  "scripts": {
    "build": "tsc", 
    "start": "node dist/webSocketServer.js", 
    "heroku-postbuild": "npm install && npm run build"
  },
  "dependencies": {
    "socket.io": "^4.0.0", 
    "@supabase/supabase-js": "^1.15.5"
  },
  "devDependencies": {
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0",
    "@types/socket.io": "^4.0.0"
  }
}